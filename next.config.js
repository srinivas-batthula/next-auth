// next.config.js
// @ts-check
/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
        // enable features if you need
    },
};

module.exports = nextConfig;
